# 接口文档

## 全局接口规范

- 请求方法：`GET`、`POST`、`PUT`、`DELETE`。
- 响应格式：
    ```json
    {
        "code": 200,
        "data": {},
        "message": "success"
    }
    ```

## 模块接口清单

### 用户模块 (`user/api/`)

- `GET /user/list`: 获取用户列表
- `POST /user/add`: 新增用户
