# 开发规范

## 代码风格

- 使用 ESLint + Prettier 强制统一代码风格。
- 变量命名采用 CamelCase，组件命名采用 PascalCase。

## 目录使用

- 模块私有内容必须放在模块目录内（如 `user/api/`）。
- 共享内容必须放在 `shared/` 目录下。

## 提交规范

- 提交信息格式：`[模块名] 描述`（如 `[user] 新增用户列表页`）。
- 禁止直接提交到 `main` 分支，必须通过 PR 审核。
